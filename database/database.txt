CREATE TABLE  "EXCEL_LOGIN" 
   (	"USERNAME" VARCHAR2(50), 
	"PASSWORD" VARCHAR2(50), 
	"EMAIL" VARCHAR2(50), 
	 CONSTRAINT "EXCEL_LOGIN_PK" PRIMARY KEY ("USERNAME") ENABLE
   )


CREATE TABLE  "DATATABLE" 
   (	"ROLL" VARCHAR2(50), 
	"NAME" VARCHAR2(50), 
	"FATHER" VARCHAR2(50), 
	"SUBJECT" VARCHAR2(500), 
	"BODY" VARCHAR2(4000)
   )